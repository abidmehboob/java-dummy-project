package org.example;
import static org.assertj.core.api.Assertions.assertThatThrownBy;
import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.BeforeEach;
import static org.junit.jupiter.api.Assertions.assertThrows;
import org.junit.jupiter.api.extension.ExtendWith;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.Test;

import org.mockito.Mockito;
import org.mockito.Mock;
import static org.mockito.Mockito.mock;
import org.mockito.junit.jupiter.MockitoExtension;

 file.
6. Include comments for each section to clearly indicate their purpose.
7. Use JUNIT5 for the test framework, MOCKITO & POWER_MOCK for mocking, and ASSERTJ for assertions.
8. Exclude `DisplayName` annotations.
9. Include necessary imports for annotations like `@ExtendWith`.
10. Ensure each test method has at least one assertion.
11
